[package]
name = "dup_finder"
version = "0.1.0"
edition = "2021"

[dependencies]
walkdir = "2.3"
sha2 = "0.10"

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1"

clap = { version = "4.1", features = ["derive"] }
hex = "0.4"

